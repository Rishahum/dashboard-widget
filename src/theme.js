
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  styles: {
    global: {
      
      body: {
        bg: '#edf2f7', // Set your desired color here
      },
    },
  },
});

export default theme;
